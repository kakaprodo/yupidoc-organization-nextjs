we are going to make the site dynamic based on the data we have in the training-center-contents.json;

the website data is based on the training-center-contents.json;

we want to remove all pages related to modules, activities and solutions(products).

Then the actual work:

- on the landing page app/[locale]/page
